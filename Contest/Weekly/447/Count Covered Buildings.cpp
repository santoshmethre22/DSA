#include<iostream>
#include<vector>
#include<map>
#include<unordered_map>
using namespace std;


class Solution {
public:
    int countCoveredBuildings(int n, vector<vector<int>>& buildings) {

        vector<int>rmin(n,INT_MAX);
        vector<int>rmax(n,INT_MIN);

        vector<int>cmin(n,INT_MAX);
        vector<int>cmax(n,INT_MIN);


        for(int i=0;i<n;i++){
            for(int j=0;j<n;j++){

                

            }
        }
        
    }
};

int main() {

    // Your code here

    return 0;
}